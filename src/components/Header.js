import React from 'react';

const Header = (props) => {
    return(
        <header className="px4">
            <h1 className="h1">🍽My Recipes</h1>
        </header>
    );
}

export default Header;